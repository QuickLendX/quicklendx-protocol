# Build outputs (all packages / nested workspaces)
target
**/target/
**/coverage/*
!**/coverage/.gitkeep
**/lcov.info

# Local diagnostics / generated logs
**/compile-errors*.log
*.profraw
*.profdata

# Local tool state
.omx/
.remember/

# Windows build artifacts (not part of the contract)
*.exe
*.pdb
